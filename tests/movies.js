const { test, todo } = QUnit;

todo("generateCardHtml(object) deve retornar html sem nenhum undefined", assert => {
    
});

todo("generateCardHtml(object) deve retornar conteúdo com undefined quando o objeto não estiver completo", assert => {

});

todo("generateCardHtml(object) deve retornar false quando não houver parâmetros", assert => {

});

todo("searchValidations(search) deve retornar mensagem de erro quando estiver vazio", assert => {

});

todo("searchValidations(search) deve retornar mensagem de erro quando tiver 1 letra", assert => {

});

todo("searchValidations(search) deve retornar true quando tiver 2 letras", assert => {

});

todo("searchValidations(search) deve retornar true quando tiver 29 letras", assert => {

});

todo("searchValidations(search) deve retornar true quando tiver 30 letras", assert => {

});

todo("searchValidations(search) deve retornar mensagem de erro quando tiver 31 letras", assert => {

});

todo("fetchMovies(string, callback) deve retornar objeto json com resultados", assert => {

});

todo("fetchMovies(string, callback) deve retornar objeto json de resultados vazio quando não encontrar o filme", assert => {

});